spring:
  datasource:
    url: jdbc:h2:mem:orion_test;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false
  jpa:
    hibernate:
      ddl-auto: create-drop

app:
  cors:
    allowed-origins: http://localhost:4200
  security:
    jwt:
      issuer: orion-test
      access-token-ttl: PT5M
      key-id: orion-test-key
      secret: MDEyMzQ1Njc4OWFiY2RlZjAxMjM0NTY3ODlhYmNkZWY=